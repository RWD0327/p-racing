# AI 작업 지침 — P-Racing

이 파일은 이 저장소에서 작업하는 모든 AI 코딩 도구를 위한 지침입니다. 자동으로 읽지 않는 도구에는 이 파일과 `PROJECT_STATUS.md`를 먼저 제공하세요.

## 작업 시작

1. `PROJECT_STATUS.md`, `WORK_LOG.md`의 최근 기록, `README.md`를 읽습니다.
2. `git status --short`, `git log -5 --oneline`으로 문서와 실제 상태가 맞는지 확인합니다. 문서보다 현재 코드·명령 결과를 우선합니다.
3. 사용자 변경을 덮어쓰지 않습니다. 대화에 없는 팀 실적·차량 제원·연락처를 만들어 넣지 않습니다.
4. 자연스러운 한국어 존댓말을 사용하고, 긴 작업 중에는 진행 상황을 짧게 알립니다.

## 진행 상황을 계속 저장하는 방법

- 새 작업을 시작할 때 `PROJECT_STATUS.md`의 진행 중 항목에 요청과 다음 단계를 적습니다.
- 의미 있는 수정·검증·배포가 끝날 때마다 현재 상태를 갱신하고 `WORK_LOG.md`에 날짜, 요청, 변경 파일, 검사 결과, 배포 여부와 다음 단계를 기록합니다.
- 중단·오류·사용자 정지 요청 시에도 완료한 내용과 재개 지점을 저장합니다. 미완료 작업을 완료로 표시하지 않습니다.
- 최종 응답 전 문서와 실제 코드·Git 상태를 대조합니다. 문서 갱신도 작업의 일부입니다.
- 시간은 확인할 수 있을 때만 기록합니다. 배포 요청 성공, 빌드 성공, 공개 URL 확인을 구분합니다.
- 비밀번호·토큰·지원서 개인정보·인증 헤더는 문서나 저장소에 기록하지 않습니다. 이 저장소는 공개입니다.
- 로그는 과거 내용을 덮어쓰지 않고 덧붙입니다. 잘못된 기록은 정정 항목으로 남깁니다.

## 유지할 동작과 범위

- 모바일과 PC 모두 한 화면에 한 섹션을 표시합니다. 휠·스와이프 한 동작에 한 섹션씩 이동합니다.
- 긴 내용은 섹션 내부에서 끝까지 읽을 수 있어야 하며, 다음 입력에서 다음 섹션으로 이동해야 합니다.
- 메뉴·키보드·모션 감소 설정·JavaScript 미실행 대체 동작을 유지합니다.
- 공식 초록 `#009B64`, 빨강 `#EE0031`, 회색 `#CDCDCD`를 중심으로 기존 디자인을 유지합니다.
- 공개 호스팅은 GitHub Pages입니다. `.openai/hosting.json`은 이전 Sites 배포 설정이므로 이를 근거로 호스팅을 바꾸거나 새 사이트를 만들지 않습니다.
- `dist/`는 생성물이 아니라 직접 편집하는 배포 소스입니다. 공개할 파일만 넣습니다.

## 검증 및 게시

- 코드 변경에 맞춰 `node --check dist/app.js`, `node --check server.mjs`, `node tests/navigation.cjs`, `node tests/security.cjs`를 실행합니다.
- 문서만 바꾸면 링크·경로·Git diff를 확인하면 됩니다. 실행하지 않은 검사를 통과로 기록하지 않습니다.
- 사용자 승인 범위 내 사이트 변경은 기존 GitHub Pages에 반영합니다. 관련 파일만 스테이징합니다.
- `main`에 커밋·푸시한 뒤 `git subtree push --prefix=dist origin gh-pages`로 게시합니다. 강제 푸시하지 않습니다.
- 문서만 변경한 경우 `main`만 푸시하며 사이트 재배포는 필요 없습니다.
- 공개 URL과 변경한 리소스를 확인한 뒤 완료를 보고합니다. 실제 브라우저·기기 검증과 Node 로직 검사를 구별합니다.

## 재개용 요청 예시

“이 저장소의 AGENTS.md와 PROJECT_STATUS.md, WORK_LOG.md를 읽고 현재 Git 상태를 확인한 뒤 작업을 이어가세요. 작업 중 진행 상황도 해당 Markdown 파일에 갱신하세요.”
